# Runtime state & secrets (AGENTS.md: never touch data/**, never commit .env)
data/
.env
.env.allow*
backups/

# Local roster seed with real Telegram IDs (README step 3, seed-file header) — never commit
config/members.local.yaml

# Python & tool caches
__pycache__/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.DS_Store
